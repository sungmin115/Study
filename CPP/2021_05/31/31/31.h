#include<iostream>
#include<string>

void run();
int add(int a, int b);
float add(float a, float b);
std::string add(std::string a, std::string b);

//계산기 만들기(더하기로만)오버로딩으로
//[사용자에게 2개의 값을 입력받아][정수,실수,문자열]